<div class="container mt-5">
  <h1>Edit Article</h1>

  <div class="card">
    <div class="card-body">
      <%= render 'form', article: @article %>

      <%= link_to 'Back to all articles', articles_path, class: "btn btn-secondary mt-3" %>
    </div>
  </div>
</div>
